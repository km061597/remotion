interface:
  display_name: "Remotion"
  short_description: "Repo-specific patterns and workflows for remotion"
  default_prompt: "Use the remotion repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true